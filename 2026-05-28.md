# 日報 — 2026-05-28

## 実施内容

### 基本情報の手動編集機能を追加（Groove CRM）

#### 変更ファイル
- `components.html`
- `app.js.html`

#### 実装内容

**1. components.html**
- 人物詳細パネル（`tpl-person-detail`）の「基本情報」セクションのタイトル横に **「✏ 編集」ボタン**を追加
- 基本情報編集用のモーダル（`tpl-modal-edit-basic-info`）を新規追加
  - 既存の基本情報をインライン編集フォームとして表示
  - 新しい項目（カスタム列）を任意に追加できる「＋ 項目を追加」ボタンを実装
  - キャンセル・保存ボタン付き

**2. app.js.html**
- `loadPersonData()` 関数で取得したデータ（`currentPersonData`, `currentPersonBasicKeys`）を `state` に保持するよう拡張
- `openPersonDetail()` に「✏ 編集」ボタンのクリックイベントを追加
- `openEditBasicInfoModal(personId)` 関数を新規実装
  - 現在の基本情報を全フィールドとしてフォームに展開
  - 既存フィールドの変更 + 新規カスタム項目の追加に対応
  - 保存時に `updatePerson(personId, patch)` GASサーバー関数を呼び出してスプレッドシートに書き込む
  - 保存完了後に基本情報パネルを自動再読み込み

#### 技術的なポイント
- GAS側の `updatePerson()` はヘッダーから列名を動的に検索する実装になっており、**フロントエンドの変更のみ**で対応完了
- 新しい項目（スプレッドシートに存在しない列名）を追加した場合も、`PeopleService.gs` の `createPerson` 相当のロジックでヘッダーが自動追加される
- `clasp push --force` でGASに反映済み
