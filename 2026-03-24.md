# 2026-03-24 作業記録

- ユーザーからInstagram API（フォロワー自動更新処理）連携のエラーについての問い合わせ対応を実施。
- エラーログ（Session has expired）を確認し、原因がFacebook/Instagram Graph APIのアクセストークン（通常60日で期限切れ）の有効期限切れであることを特定。

* 新しいトークンを取得して、Config（または設定画面）の `instagram_access_token` を更新するように案内した。
* また、作成したその日にセッションが切れた理由について、取得したトークンが「短期アクセストークン（有効期限1時間）」であった可能性が高いため、「長期アクセストークン（有効期限60日）」へ変換する手順が必要であることを説明した。
* 追加で「Instagram Business Account ID（`account_id`）」の取得方法について問い合わせがあり、Graph APIエクスプローラを使った取得手順を案内した。

- ユーザーから「データの自動追加機能」の追加要望を受領し、詳細な要件（どのようなデータを、どこから、どのようなトリガーで追加するか）のヒアリングを開始した。
- 要件確認後、インフルエンサー情報の登録機能として以下の実装を行った。
  - フロントエンド: `components.html` に「＋新規登録」ボタンと情報入力用モーダルのテンプレート（`tpl-modal-add-person`）を追加。`app.js.html` にモーダルの表示・送信処理を実装。
  - バックエンド: `PeopleService.gs` に `createPerson` 関数を追加し、指定区分（シート名）の末尾に行を追加する処理を実装。
- ローカルからの `clasp push` が権限エラーで失敗したため、ユーザーへ直接のアップロードと動作検証を依頼。
- ユーザーから「シートの項目以外にも追加できるように」との追加要望を受領。動的なカスタム項目追加機能を実装した。
  - フロントエンド: 登録モーダルに「＋項目を追加」ボタンと、自由に入力フィールドを追加して送信するUIを実装。
  - バックエンド: 送信されたデータのうちスプレッドシートの1行目（ヘッダー）に存在しない項目名がある場合、自動で新しい列としてヘッダーを拡張しデータを保存する処理を追加。

- ユーザーからInstagramフォロワー数自動更新時の `(#100) Tried accessing nonexisting field (business_discovery)` エラーについて問い合わせを受領。
- 原因として、「対象者が個人アカウント/非公開アカウントである」または「ConfigシートのビジネスアカウントIDの誤り・アカウントのプロ設定解除」のいずれかであることを特定し、それぞれの確認方法と対処法を案内した。
- `instagram_business_account_id` が誤っている可能性があるとのご相談を受けたため、正しいID（15桁以上の数字）の再取得手順を案内した。
- 再設定後、新たに `Invalid user id (code: 110)` のエラーが発生したとの連絡を受領。エラー内容をデコード（解析）した結果、「ユーザーネームが〇〇のユーザーが見つかりません」というAPI仕様通りの返答であることを特定。APIの連携自体は成功しており、該当アカウント自体のID変更や削除が原因であることを解説した。
- 「シートにあるURLで更新できないのか？」という追加質問に対し、すでにシステムはシートのURLからアカウント名（ID）を抽出して情報を取りに行っているが、そのURL（リンク）自体が相手のID変更等により無効になっている旨を説明した。
- 「シートにあるURLで更新できないのか？」という追加質問に対し、すでにシステムはシートのURLからアカウント名（ID）を抽出して情報を取りに行っているが、そのURL（リンク）自体が相手のID変更等により無効になっている旨を説明した。
- その後、ユーザー側でコードを修正してテスト実行（`v19.0` への書き換えやデバッグログ追加など）を行った際に、`Exception: Invalid argument` エラーが発生。原因がURL構築時の `encodeURIComponent` の欠落（`{}` などの特殊文字がURLエンコードされていないため UrlFetchApp が弾いたこと）であると特定し、修正コードを提示した。
- 実際に設定が成功して取得できるのかを確認したいとの要望を受け、必ず存在する公式アカウント（Starbucks等）を対象とした検証用テストコードを作成し提供した。
- ユーザーより提供したテストコードで「Configシートが見つかりません」というエラーが出たとの報告。実際のシート名が `99_Config` であったためのシート名指定ミスであることを指摘し、コードの修正箇所を案内した。
- ユーザーから「人物詳細画面の基本情報で、シートの項目と同じ項目を表示するようにしてほしい」との要望を受領。`app.js.html` を修正し、シート側のヘッダー項目を自動取得して表示するよう動的化を実施した（CRM管理用の重複項目とシステム領域は自動除外する機能も付加）。その後 `clasp push` にて反映を完了。
- ユーザーから「CRMシステムのデバッグ・検証」の依頼を受けて、デプロイ済みWebアプリ（`https://script.google.com/...`）をブラウザで開き検証を実施。ナビゲーション・検索フォーム・テーブル・管理画面などUIは全て健全に描画され、システムの崩壊は確認されなかった。検索結果が0件となった点はGoogle認証が通っていない状態（ゲストアクセス）で検証したためであり、ユーザー自身のログイン済みブラウザで動作確認を依頼した。
- ユーザーから「個人アカウントのフォロワー数も取得したい」との要望。Graph APIは仕様上プロアカウント（ビジネス/クリエイター）のみ対応のため、**Webスクレイピングによるフォールバック機能**を `FollowerUpdateService.gs` に実装した。処理フロー: ①Graph APIで取得試行→②失敗時にInstagramプロフィールページのHTMLを取得→③meta description / 埋め込みJSON / interactionStatistic の3パターンでフォロワー数を抽出。英語（K/M表記）・日本語（万表記）両方に対応する `_parseFollowerString()` も追加。`clasp push` で反映済み。
