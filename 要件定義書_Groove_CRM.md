# 要件定義書

## Groove CRM — タレント・インフルエンサー管理システム

| 項目 | 内容 |
|------|------|
| 文書バージョン | 1.0 |
| 作成日 | 2026-05-30 |
| 対象システム | Groove CRM |
| ステータス | 正式版 |

---

## 目次

1. [システム概要・背景・目的](#1-システム概要背景目的)
2. [対象ユーザー](#2-対象ユーザー)
3. [システムスコープ](#3-システムスコープ)
4. [機能要件](#4-機能要件)
5. [非機能要件](#5-非機能要件)
6. [データモデル](#6-データモデル)
7. [権限管理（RBAC）](#7-権限管理rbac)
8. [外部API依存](#8-外部api依存)
9. [システムアーキテクチャ](#9-システムアーキテクチャ)
10. [制約条件](#10-制約条件)
11. [セットアップ・運用要件](#11-セットアップ運用要件)
12. [将来の拡張・課題](#12-将来の拡張課題)

---

## 1. システム概要・背景・目的

### 1.1 背景

タレント事務所・キャスティング会社では、モデル・インフルエンサー・スポーツ選手・著名人といった多様な人物を日常的に管理する必要がある。従来のスプレッドシート管理では、次のような課題があった。

- 人物ごとの接触履歴・次アクションが把握しにくい
- 案件とキャスティング候補者の紐付けが散在する
- フォロワー数・事務所情報などのSNS情報の手動更新に工数がかかる
- 担当者間で情報共有が非効率

### 1.2 目的

これらの課題を解決するため、**Google スプレッドシートを既存データベースとして活用しながら、CRM 機能を付加した Web アプリケーション**を構築する。

### 1.3 システム名

**Groove CRM（グルーブ CRM）**

---

## 2. 対象ユーザー

| ユーザー区分 | 想定利用者 | 主な用途 |
|------------|-----------|---------|
| Admin | システム管理者・マネージャー | 権限設定・インデックス管理・バックアップ・データ検証 |
| Editor | 営業担当・キャスティング担当 | 人物情報更新・活動ログ追加・案件管理 |
| Viewer | 閲覧専用スタッフ・外部参照者 | 人物検索・情報閲覧のみ |

---

## 3. システムスコープ

### 3.1 対象範囲（In Scope）

- モデル / インフルエンサー / スポーツ選手・著名人 の人物情報管理
- キャスティング案件管理
- 活動ログ（接触履歴）管理
- SNS情報の自動スクレイピング・フォロワー数自動更新
- AI（Gemini）を用いた自然言語検索
- 権限管理（3段階ロール）
- 週次自動バックアップ

### 3.2 対象外（Out of Scope）

- 外部クライアントへの公開ポータル
- 請求書・契約書の電子管理
- 給与・報酬管理
- モバイルアプリ（iOS / Android ネイティブ）

---

## 4. 機能要件

### 4.1 人物管理

#### 4.1.1 検索・閲覧

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-P-01 | フリーワード検索 | 名前・ユーザー名・タグ・メモ等、全64列を対象とした AND 部分一致検索 | Viewer |
| F-P-02 | カテゴリフィルタ | モデル / インフルエンサー / スポーツ選手・著名人 の区分絞り込み | Viewer |
| F-P-03 | ステータスフィルタ | 未着手 / アプローチ中 / 交渉中 / 契約済 / NG での絞り込み | Viewer |
| F-P-04 | 優先度フィルタ | 高 / 中 / 低 での絞り込み | Viewer |
| F-P-05 | 担当者フィルタ | 社内担当者名での絞り込み | Viewer |
| F-P-06 | 所在地フィルタ | 居住地域（都道府県）での絞り込み | Viewer |
| F-P-07 | フォロワー数フィルタ | フォロワー数の範囲指定（最小〜最大）での絞り込み | Viewer |
| F-P-08 | ギャラ目安フィルタ | ギャラ目安の範囲指定での絞り込み | Viewer |
| F-P-09 | 所属事務所フィルタ | 所属事務所名（部分一致）での絞り込み | Viewer |
| F-P-10 | タグフィルタ | タグ（部分一致）での絞り込み | Viewer |
| F-P-11 | 次アクション期限フィルタ | 期限切れ / 今日 / 今週 のクイックフィルタ | Viewer |
| F-P-12 | ページング | 1ページ50件単位の結果表示 | Viewer |
| F-P-13 | ソート | 任意列・昇降順でのソート | Viewer |
| F-P-14 | 人物詳細表示 | 基本情報 + CRM情報を右ペインに表示 | Viewer |

#### 4.1.2 編集

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-P-15 | CRM情報更新 | ステータス / 優先度 / 担当 / 次アクション日・内容 / メモ の編集 | Editor |
| F-P-16 | 人物追加 | 新規人物をいずれかの人物シートに追加 | Editor |

### 4.2 AI アシスタント

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-AI-01 | 自然言語検索 | ユーザーの日本語入力を Gemini API で解析し、検索パラメータを自動抽出して検索を実行 | Viewer |
| F-AI-02 | 検索パラメータ抽出 | keyword / category / followersMin / followersMax / location / tag / platform / age / agency / owner を抽出 | Viewer |

**解析対象パラメータ:**

| パラメータ | 型 | 例 |
|-----------|----|----|
| keyword | string | 名前・ユーザー名・タグ等の単語 |
| category | string | モデル / インフルエンサー / スポーツ選手・著名人 |
| followersMin | number | フォロワー数下限 |
| followersMax | number | フォロワー数上限 |
| location | string | 所在地（都道府県） |
| tag | string | TikToker / YouTuber / グルメ 等 |
| platform | string | Instagram / TikTok / YouTube / X(Twitter) |
| ageMin | number | 年齢下限 |
| ageMax | number | 年齢上限 |
| agency | string | 有 / 無 |
| owner | string | 担当者名 |

### 4.3 案件管理

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-D-01 | 案件一覧表示 | 全案件のリスト表示（フィルタ: 状態 / 担当 / キーワード） | Viewer |
| F-D-02 | 案件作成 | 案件名 / クライアント / 状態 / 担当 / 納期 を入力してモーダルから作成 | Editor |
| F-D-03 | 案件詳細表示 | 案件情報 + キャスティング候補者一覧の表示 | Viewer |
| F-D-04 | 候補者追加 | 人物検索から案件へ候補者として追加（重複チェック付き） | Editor |
| F-D-05 | キャスティング更新 | 候補ステータス / 提示条件 / NG理由 / メモ の部分更新 | Editor |

### 4.4 活動ログ

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-L-01 | ログ追加 | 種別（電話/メール/DM/面談/その他）＋ 内容 / 結果 / 次アクション を記録 | Editor |
| F-L-02 | タイムライン表示 | 人物に紐づく活動履歴を時系列（新しい順）で表示 | Viewer |
| F-L-03 | 人物情報自動更新 | ログ追加時に最終接触日・次アクション日・次アクション内容・ステータスを自動更新 | Editor |
| F-L-04 | インデックス同期 | ログ追加後に 90_Index の該当行も自動同期 | Editor |

### 4.5 スクレイピング・自動データ収集

#### 4.5.1 人物情報スクレイピング（PersonInfoScraper.gs）

| 機能ID | 機能名 | 説明 | 対象 |
|--------|-------|------|------|
| F-S-01 | 人物情報一括スクレイピング | Wikipedia・refetter・DuckDuckGo から人物属性を自動取得（全件バッチ処理） | Editor/Admin |
| F-S-02 | 選択行スクレイピング | スプレッドシート上で選択した行のみスクレイピング | Editor/Admin |
| F-S-03 | 自動スクレイピング | 名前列への入力を検知して自動実行（installable trigger） | 自動 |
| F-S-04 | 続きから再実行 | バッチ途中で中断した場合に続きから再開 | Admin |

**取得情報と書き込みポリシー:**

| 情報 | 列番号 | 書き込みポリシー | 取得元 |
|------|--------|----------------|-------|
| 所在地 | 6 | 空欄のみ書き込み | Wikipedia / refetter |
| Instagram URL | 8 | 空欄のみ書き込み | refetter |
| 所属事務所 | 29 | 空欄のみ書き込み | Wikipedia / refetter |
| 性別 | 61 | 常に上書き | Wikipedia |
| Twitter/X URL | 62 | 常に上書き | Wikipedia / DuckDuckGo |
| YouTube URL | 63 | 常に上書き | YouTube Data API / DuckDuckGo |
| TikTok URL | 64 | 常に上書き | DuckDuckGo |
| その他SNS URL | 65 | 常に上書き | DuckDuckGo |
| IGアイコン画像URL | 66 | 常に上書き | Instagram og:image |

**データ取得の優先順位:**

```
1. Wikipedia API（主ソース・APIキー不要）
   └ MediaWiki Search API → REST Summary API → 外部リンク抽出
2. insta.refetter.com（補完）
   └ bio テキスト・所在地・SNS リンクを抽出
3. DuckDuckGo Lite HTML（フォールバック）
4. Google HTML（最終フォールバック）
5. YouTube Data API（オプション・APIキー要）
6. Instagram アイコン（og:image 取得）
```

#### 4.5.2 フォロワー数自動更新（FollowerUpdateService.gs）

| 機能ID | 機能名 | 説明 |
|--------|-------|------|
| F-F-01 | フォロワー数更新 | insta.refetter.com からフォロワー数・所在地を自動スクレイピング（バッチ処理） |

### 4.6 管理機能

| 機能ID | 機能名 | 説明 | 最低権限 |
|--------|-------|------|---------|
| F-A-01 | インデックス再構築 | 3人物シートから `90_Index` を全削除→再生成（冪等） | Admin |
| F-A-02 | データ検証 | person_id重複 / URL重複 / 必須項目欠落の一括チェック | Admin |
| F-A-03 | 初期セットアップ | 必要シート・ヘッダの自動作成 | Admin |
| F-A-04 | 権限設定 | Admin / Editor / Viewer のメールアドレス管理 | Admin |
| F-A-05 | 週次バックアップ | スプレッドシートを Google Drive に複製保存（毎週月曜 6:00 自動実行） | Admin |

---

## 5. 非機能要件

### 5.1 性能要件

| 項目 | 要件 |
|------|------|
| 検索レスポンス | キャッシュヒット時: 1秒以内 / キャッシュミス時: 10秒以内 |
| ページング | 1ページ50件表示 |
| キャッシュ戦略 | Index データ: TTL 10分、検索結果: TTL 10分、Config: TTL 5分 |
| バッチ上限 | 1バッチ最大10件 or 5分（GAS 6分制限の安全マージン） |

### 5.2 セキュリティ要件

| 項目 | 要件 |
|------|------|
| 認証 | Google アカウント認証（GAS WebApp 経由） |
| 権限制御 | `99_Config` シートのメールアドレスリストで 3段階ロール判定 |
| APIキー管理 | Gemini API キーは GAS スクリプトプロパティで管理（ソースコード非埋め込み） |
| アクセス制御 | スプレッドシートへのアクセス権がないユーザーは利用不可 |

### 5.3 可用性・信頼性要件

| 項目 | 要件 |
|------|------|
| 利用時間 | Google Apps Script の稼働時間に依存（SLA なし） |
| バックアップ | 毎週月曜 6:00 に Google Drive へ自動バックアップ |
| データ整合性 | インデックス再構築・データ検証機能により整合性を担保 |

### 5.4 拡張性・保守性要件

| 項目 | 要件 |
|------|------|
| デプロイ | clasp CLI によるローカル↔GAS 間のコード同期 |
| バージョン管理 | Git / GitHub によるソースコード管理 |
| コード構成 | 機能ごとにサービスファイルを分離（単一責任） |

---

## 6. データモデル

### 6.1 人物マスターシート（3種共通構造）

| シート名 | 区分ラベル |
|---------|-----------|
| `モデル` | モデル |
| `インフルエンサー` | インフルエンサー |
| `スポーツ選手・著名人` | スポーツ選手・著名人 |

**主要列定義（0-indexed）:**

| 列番号 | 列名 | 説明 | 備考 |
|--------|------|------|------|
| 0 | フォロワー数 | Instagram フォロワー数 | 自動更新対象 |
| 1 | ユーザー名 | Instagram ユーザー名 | |
| 2 | 名前 | 氏名 | 必須 |
| 6 | 所在地 | 居住地（都道府県） | スクレイピング取得 |
| 8 | URL | Instagram プロフィール URL | |
| 16 | person_id | CRM一意識別子（P + タイムスタンプ + ランダム） | CRM追加列 |
| 26 | サブカテゴリ | 細分類 | |
| 27 | タグ | 検索用タグ | |
| 28 | 社内担当 | 担当者名 | |
| 29 | 所属事務所 | 所属事務所名 | スクレイピング取得 |
| 36 | ギャラ目安 | 出演料目安 | |
| 49 | ステータス | CRM 進捗ステータス | 未着手/アプローチ中/交渉中/契約済/NG |
| 51 | 最終接触日 | 最終コンタクト日 | ログ追加時に自動更新 |
| 52 | 次アクション日 | 次回アクション予定日 | ログ追加時に自動更新 |
| 53 | 次アクション内容 | アクション内容 | |
| 54 | 優先度 | 優先度（高/中/低） | |
| 59 | メモ | 備考メモ | |
| 61 | 性別 | 性別 | スクレイピング取得 |
| 62 | Twitter | Twitter/X URL | スクレイピング取得 |
| 63 | YouTube | YouTube チャンネル URL | スクレイピング取得 |
| 64 | TikTok | TikTok URL | スクレイピング取得 |
| 65 | その他SNS | note.com 等 | スクレイピング取得 |
| 66 | IGアイコン | Instagram プロフィール画像URL | スクレイピング取得 |

### 6.2 `90_Index`（統合検索インデックス）

3人物シートのデータを集約した高速検索用インデックス。

| 列名 | 説明 |
|------|------|
| person_id | 人物一意ID |
| 区分 | モデル / インフルエンサー / スポーツ選手・著名人 |
| 名前 | 氏名 |
| ユーザー名 | SNS ユーザー名 |
| URL | プロフィール URL |
| フォロワー数 | SNS フォロワー数 |
| ギャラ目安 | 報酬目安 |
| 身長(cm) | 身長 |
| 所在地 | 居住地域 |
| サブカテゴリ | 細分類 |
| タグ | 検索用タグ |
| ステータス | CRM 進捗ステータス |
| 最終接触日 | 最後に連絡した日付 |
| 次アクション日 | 次の行動予定日 |
| 優先度 | 高 / 中 / 低 |
| 年齢 | 年齢 |
| メインプラットフォーム | 主要SNSプラットフォーム |
| 所属事務所 | 所属事務所名 |
| 担当（社内） | 社内担当者名 |
| source_sheet | 元シート名（内部参照用） |
| source_row | 元シートの行番号（内部参照用） |
| _search_text | 全列を結合したフリーワード検索用テキスト |

### 6.3 `10_Deals`（案件マスター）

| 列名 | 型 | 説明 |
|------|------|------|
| deal_id | number | 案件ID（MAX+1 自動採番） |
| 案件名 | string | 案件名称（必須） |
| クライアント | string | クライアント名 |
| 状態 | string | 企画中 / 提案中 / 進行中 / 完了 / 中止 |
| 担当 | string | 社内担当者名 |
| 納期 | date | 納期日（YYYY-MM-DD） |

### 6.4 `11_DealCasting`（キャスティング候補）

| 列名 | 型 | 説明 |
|------|------|------|
| deal_casting_id | number | キャスティングID（MAX+1 自動採番） |
| deal_id | number | 紐づく案件ID |
| person_id | string | 紐づく人物ID |
| 候補ステータス | string | 候補 / 交渉中 / 決定 / NG 等 |
| 提示条件 | string | 報酬・条件 |
| NG理由 | string | 不採用理由 |
| メモ | string | 備考 |

### 6.5 `20_ActivityLog`（活動ログ）

| 列名 | 型 | 説明 |
|------|------|------|
| log_id | number | ログID（MAX+1 自動採番） |
| person_id | string | 紐づく人物ID |
| 日時 | datetime | 活動日時（YYYY-MM-DD HH:mm） |
| 種別 | string | 電話 / メール / DM / 面談 / その他 |
| 内容 | string | 活動の内容 |
| 結果 | string | 活動の結果 |
| 次アクション日 | date | 次アクション予定日 |
| 次アクション内容 | string | 次アクションの内容 |
| 担当 | string | 実施した担当者 |

### 6.6 `99_Config`（システム設定）

| key | 説明 |
|-----|------|
| admin_emails | Admin 権限のメールアドレス（カンマ区切り） |
| editor_emails | Editor 権限のメールアドレス（カンマ区切り） |
| viewer_emails | Viewer 権限のメールアドレス（カンマ区切り） |
| instagram_access_token | Instagram API アクセストークン |
| instagram_business_account_id | Instagram ビジネスアカウントID |

### 6.7 ID採番ルール

| 対象 | 方式 | 形式例 |
|------|------|-------|
| person_id | UUID-like（P + タイムスタンプ + ランダム） | `Pm1abc123xyz` |
| deal_id | MAX+1 整数連番 | `1`, `2`, `3`... |
| deal_casting_id | MAX+1 整数連番 | `1`, `2`, `3`... |
| log_id | MAX+1 整数連番 | `1`, `2`, `3`... |

---

## 7. 権限管理（RBAC）

### ロール階層

```
Admin (Lv.3) > Editor (Lv.2) > Viewer (Lv.1) > None (Lv.0)
```

| ロール | レベル | 許可操作 |
|--------|--------|---------|
| **Admin** | 3 | 全操作 + Config 管理 + インデックス再構築 + データ検証 + バックアップ + スクレイピング管理 |
| **Editor** | 2 | 人物 CRM 情報更新 + 活動ログ追加 + 案件 CRUD + キャスティング管理 + 人物追加 |
| **Viewer** | 1 | 検索 + 閲覧 + AI検索のみ |
| **None** | 0 | アクセス不可 |

### 権限判定フロー

1. `99_Config` シートの `admin_emails` / `editor_emails` / `viewer_emails` にメールアドレスを登録
2. GAS が `Session.getActiveUser().getEmail()` でログインユーザーを取得
3. 各リストに含まれるかチェックし、最上位ロールを付与
4. スプレッドシートにアクセスできるユーザーは最低 Viewer
5. Config は `CacheService` で 5分間キャッシュ

---

## 8. 外部API依存

| API | 用途 | 認証方式 | 必須/オプション |
|-----|------|---------|--------------|
| **Gemini 1.5 Flash** | AI自然言語検索（クエリ解析） | スクリプトプロパティ `GEMINI_API_KEY` | オプション |
| **insta.refetter.com** | フォロワー数・人物情報スクレイピング | 不要（HTMLスクレイピング） | 必須（フォロワー更新時） |
| **Wikipedia API（MediaWiki）** | 人物属性取得（主ソース） | 不要 | 必須（スクレイピング時） |
| **DuckDuckGo Lite** | Web 検索フォールバック | 不要 | 必須（スクレイピング時） |
| **Google Custom Search API** | Web 検索（オプション） | `CUSTOM_SEARCH_API_KEY` | オプション |
| **YouTube Data API v3** | YouTube チャンネル検索 | `YOUTUBE_API_KEY` | オプション |

> [!WARNING]
> 外部サイトのスクレイピングはサイト側の仕様変更により動作しなくなる場合があります。定期的な動作確認が必要です。

---

## 9. システムアーキテクチャ

### 9.1 技術スタック

| レイヤー | 技術 |
|---------|------|
| フロントエンド | HTML / CSS / Vanilla JavaScript（SPA） |
| バックエンド | Google Apps Script（V8 ランタイム） |
| データベース | Google スプレッドシート |
| デプロイ | GAS Web App + clasp CLI |
| キャッシュ | CacheService（チャンク分割、TTL: 10分） |
| バージョン管理 | Git / GitHub |

### 9.2 アーキテクチャ構成図

```
┌──────────────────────────────────────────────────────┐
│                  ブラウザ (SPA)                        │
│  index.html + components.html + app.js.html           │
│  ・ハッシュルーティング (#/, #/deals, #/admin)         │
│  ・google.script.run でサーバー関数を呼び出し          │
└──────────────────────┬───────────────────────────────┘
                       │ google.script.run.*()
┌──────────────────────▼───────────────────────────────┐
│              Google Apps Script（サーバー側）          │
│                                                       │
│  Main.gs          — 定数・エントリポイント・ユーティリティ │
│  Auth.gs          — 権限管理・Config 設定              │
│  IndexService.gs  — 検索インデックス構築・検索          │
│  PeopleService.gs — 人物 CRUD                         │
│  DealService.gs   — 案件・キャスティング管理            │
│  LogService.gs    — 活動ログ・人物情報同期              │
│  GeminiService.gs — Gemini API 連携（AI検索）          │
│  FollowerUpdateService.gs — フォロワー数自動更新        │
│  AgencyUpdateService.gs  — 事務所情報更新              │
│  WeeklyBackup.gs  — 週次バックアップ                  │
│                                                       │
│  ─── CacheService（10分 TTL）────────────────────     │
└──────────────────────┬───────────────────────────────┘
                       │ SpreadsheetApp API
┌──────────────────────▼───────────────────────────────┐
│            Google スプレッドシート（DB）               │
│  モデル / インフルエンサー / スポーツ選手・著名人        │
│  90_Index（統合検索インデックス）                       │
│  10_Deals / 11_DealCasting / 20_ActivityLog           │
│  99_Config（権限・API設定）                            │
└──────────────────────────────────────────────────────┘
```

### 9.3 SPAルーティング

| ルート | ビュー | テンプレートID |
|--------|-------|--------------|
| `#/` | 人物検索 + 詳細 | `tpl-search-view` + `tpl-person-detail` |
| `#/deals` | 案件一覧 | `tpl-deal-list` |
| `#/deals/:id` | 案件詳細 | `tpl-deal-detail` |
| `#/admin` | 管理画面 | `tpl-admin-view` |

### 9.4 バックエンドAPIリファレンス

フロントエンドから `google.script.run` で呼び出すサーバー関数一覧。

**共通レスポンス形式:**
```javascript
{ success: boolean, data: any, message: string }
```

#### 人物検索・更新

| 関数名 | パラメータ | 戻り値 | 権限 |
|--------|-----------|-------|------|
| `searchPeople(params)` | `{ keyword, category, location, status, priority, owner, nextActionFilter, followersMin, followersMax, page, pageSize }` | `{ total, page, pageSize, results[] }` | Viewer |
| `getPerson(personId)` | `string` | `{ sheetName, rowIndex, data }` | Viewer |
| `updatePerson(personId, patch)` | `string, { 列名: 新値 }` | — | Editor |
| `addPerson(payload)` | `Object` | — | Editor |

#### 案件管理

| 関数名 | パラメータ | 戻り値 | 権限 |
|--------|-----------|-------|------|
| `listDeals(filters)` | `{ status, owner, keyword }` | `Deal[]` | Viewer |
| `createDeal(payload)` | `{ 案件名, クライアント, 状態, 担当, 納期 }` | `{ deal_id }` | Editor |
| `getDeal(dealId)` | `number\|string` | `Deal + castings[]` | Viewer |
| `addPersonToDeal(dealId, personId, initStatus)` | `string, string, string?` | `{ deal_casting_id }` | Editor |
| `updateDealCasting(dealCastingId, patch)` | `string, { 列名: 値 }` | — | Editor |

#### 活動ログ

| 関数名 | パラメータ | 戻り値 | 権限 |
|--------|-----------|-------|------|
| `addActivityLog(payload)` | `{ person_id, 種別, 内容, 結果, 次アクション日, 次アクション内容, 担当, ステータス }` | `{ log_id }` | Editor |
| `getPersonTimeline(personId)` | `string` | `Log[]`（新しい順） | Viewer |

#### AI・管理

| 関数名 | 説明 | 権限 |
|--------|------|------|
| `parseSearchQueryWithAI(userInput)` | 自然言語クエリを解析して検索パラメータを返す | Viewer |
| `rebuildIndex()` | 90_Index を全再構築 | Admin |
| `validateData()` | データ整合性チェック | Admin |
| `setupSheets()` | 初期シート・ヘッダ作成 | Admin |
| `getCurrentUser()` | ユーザー情報取得 | 制限なし |
| `getConfig()` | Config 全取得 | Admin |
| `updateConfig(patch)` | Config 更新 | Admin |
| `createWeeklyBackup()` | スプレッドシート複製保存 | Admin |

### 9.5 キャッシュ戦略

| キャッシュ対象 | キー | TTL |
|--------------|------|-----|
| 90_Index 全データ | `CRM_INDEX_CACHE_0` 〜 `_9`（90KBチャンク分割） | 10分 |
| 検索結果 | `CRM_SEARCH_{クエリハッシュ}` | 10分 |
| Config 設定 | `CRM_CONFIG_CACHE` | 5分 |

- 更新系操作（人物更新・ログ追加・インデックス再構築）後にキャッシュ一括クリア
- 90KB超の検索結果はキャッシュ保存をスキップ

---

## 10. 制約条件

### 10.1 Google Apps Script の制約

| 制約項目 | 上限値 |
|---------|--------|
| スクリプト実行時間 | 6分 / 1回（バッチは5分で安全停止） |
| CacheService 1件あたりのサイズ | 100KB |
| CacheService の TTL最大値 | 6時間（21,600秒） |
| UrlFetchApp の呼び出し回数 | 20,000回 / 日 |
| SpreadsheetApp の読み書き回数 | 30,000回 / 日 |

### 10.2 スクレイピングの制約

- 外部サイト（refetter / Wikipedia / DuckDuckGo）の仕様変更でスクレイピングが機能しなくなる場合がある
- 大量アクセスによるブロックリスクがある（バッチ処理にウェイトを設定することで緩和）

### 10.3 スプレッドシートの制約

- 列数が増えると検索インデックスのデータサイズが増大し、キャッシュ効率が低下する
- スプレッドシートの最大行数は 1,000万セル

---

## 11. セットアップ・運用要件

### 11.1 前提条件

- Node.js インストール済み
- clasp CLI インストール済み（`npm i -g @google/clasp`）
- Google アカウントで `clasp login` 済み

### 11.2 初回セットアップ手順

```bash
# 1. リポジトリクローン
git clone https://github.com/teameffectors4-bit/Groove_CRM.git
cd Groove_CRM

# 2. .clasp.json の scriptId を自分の GAS プロジェクト ID に変更

# 3. GAS プロジェクトへプッシュ
clasp push
```

GAS エディタでの手動実行:

1. `setupSheets()` — 初期シート・ヘッダ作成
2. `99_Config` シートにロール別メールアドレスを登録
3. `GEMINI_API_KEY` をスクリプトプロパティに設定（AI 機能使用時）
4. `rebuildIndex()` — インデックス初期構築
5. スクレイピング列ヘッダ設定: メニュー「スクレイピング → 【初回】ヘッダを設定」
6. 自動スクレイピング有効化: メニュー「スクレイピング → 【自動】トリガーを登録」
7. 週次バックアップ有効化: `setupWeeklyBackupTrigger()` を手動実行
8. GAS エディタ → デプロイ → 新しいデプロイ → ウェブアプリとして公開

### 11.3 日常的な開発コマンド

```bash
clasp push   # ローカル → GAS
clasp pull   # GAS → ローカル
clasp open   # GAS エディタを開く
```

### 11.4 バックアップ設定

```javascript
// GAS エディタで1回実行 → 毎週月曜 6:00 自動実行
setupWeeklyBackupTrigger();

// トリガー解除
removeWeeklyBackupTrigger();
```

### 11.5 選択肢マスター

| フィールド | 選択肢 |
|-----------|-------|
| 人物ステータス | 未着手 / アプローチ中 / 交渉中 / 契約済 / NG |
| 優先度 | 高 / 中 / 低 |
| 案件状態 | 企画中 / 提案中 / 進行中 / 完了 / 中止 |
| 活動種別 | 電話 / メール / DM / 面談 / その他 |

---

## 12. 将来の拡張・課題

### 12.1 既知の技術的課題

| 課題 | 詳細 | 対応方針 |
|------|------|---------|
| Git マージコンフリクト | `Main.gs` に HEAD/dev ブランチのコンフリクトマーカーが残存 | マージを解消し正常なコードに統合 |
| スクレイピング安定性 | 外部サイト仕様変更による動作停止リスク | 定期的な動作確認と代替APIの検討 |
| スプレッドシートの性能限界 | データ増加に伴う検索速度の低下 | 将来的な DB 移行を検討 |

### 12.2 将来の機能拡張候補

| 拡張機能 | 説明 |
|---------|------|
| Instagram API 連携 | フォロワー数・エンゲージメント率の公式API取得 |
| Excel エクスポート | 案件・人物情報の xlsx 出力（SheetJS ライブラリは既に組み込み済み） |
| 通知機能 | 次アクション日になったらメール・Slack 通知 |
| モバイル最適化 | レスポンシブデザインのさらなる強化 |
| 外部 DB 移行 | データ増加に備えた Firebase / Supabase への移行 |
| キャスティング費用管理 | 案件ごとのコスト管理機能 |

---

*本ドキュメントは Groove CRM の現行実装を元に作成されました。*
*システムの変更に合わせて随時更新してください。*
