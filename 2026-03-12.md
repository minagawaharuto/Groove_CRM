# 2026-03-12

## 本日行った作業
- **フォロワー数自動更新機能の追加**
  - **対象SNS**: Instagram
  - **更新頻度**: 週1回
  - `FollowerUpdateService.gs` を新規作成し、人物シートにあるURL（9列目）からInstagramアカウントを検知して Graph API を利用してフォロワー数を取得する処理 `updateAllFollowers()` を実装。
  - 毎週日曜の3時台に自動実行されるようなトリガー設定関数 `setupWeeklyFollowerUpdateTrigger()` を実装。
  - `Main.gs` の `setupSheets()` 関数を修正し、`99_Config` シートに `instagram_access_token` と `instagram_business_account_id` の入力枠が自動作成されるように変更。
  - APIキーを利用するための利用準備と設定手順に関する Walkthrough ドキュメントを作成。
- **Graph API Explorer の操作説明**
  - Facebook Graph API Explorerの画面スクショを受け取り、アクセストークンの発行手順と必要なパーミッションについて案内しサポート。
  - Meta for Developersのアプリダッシュボード画面にて、「Instagram」の製品を追加する手順について案内しサポート。
  - アプリのタイプが「ビジネス」であるため、ユースケースの追加画面からInstagram Graph APIを設定するよう案内を修正。
  - 「Invalid platform app」エラーに関連して、アプリアイコンやプライバシーポリシーのURL等の不足する基本設定の入力手順をサポート。
  - プラットフォーム（ウェブサイト）の追加手順について案内。
  - Facebookログインの「有効なOAuthリダイレクトURI」にて `https://developers.facebook.com/` と入力すると「Facebook URLではありません」と弾かれる仕様変更への対応手順を案内。
  - 「アプリの役割」画面の確認を実施し、権限設定に問題がないことを確認。
  - エラー解消後、再度グラフAPIエクスプローラでのアクセストークン生成から長期化までの手順を改めて説明。
  - 「グラフAPIエクスプローラ」での最終的な「長期アクセストークン」と「ビジネスアカウントID」の取得手順をさらに詳細にステップバイステップで案内。
- **Configシートの設定説明**
  - `99_Config`シートにInstagram設定用の行が不足している状況の画像を確認し、手動で行を追加してキーと値を入力する手順を案内。
- **ビジネスアカウントID取得の説明とトラブルシュート**
  - グラフAPIエクスプローラを用いて `instagram_business_account_id` を確認するためのクエリ方法を改めて具体的に回答。
  - `"data": []` と空の結果が返ってきている画像を確認し、Instagramアカウントがプロフェッショナルアカウントになっていないか、Facebookページとリンクされていない（またはアクセストークン発行時に権限を選択していない）ことが原因であることを案内。
  - 「Invalid OAuth 2.0 Access Token」のエラーについて、アクセストークン欄が空であることおよび「POST」メソッドになっている箇所の修正手順を案内。
  - 「Tried accessing nonexisting field (accounts)」のエラーについて、画面が見切れているためおそらく「アプリトークン」になっていると推測し、「ユーザーアクセストークン」を選択し直すよう案内。
  - ユーザーアクセストークン取得後、「data: []」と表示される問題について、`instagram_basic` 権限の不足と、認証再実行時の該当Facebookページへのチェック追加の必要性を案内。
  - `instagram_basic` が見つからない場合の対処（検索入力）および、FacebookページとInstagramのリンク状態の再確認を案内。
  - プロアカウント変更およびFBページ連携を行っても `[ ]` のままになる問題について、別ルートでのページID取得からInstagramビジネスアカウントを取得する方法を案内。
  - 「Invalid OAuth access token」のエラーについて、送信先URLが `graph.instagram.com` に戻ってしまっている点が原因であると指摘し、`graph.facebook.com` への修正とトークン再生成を案内。
  - `me/accounts`に対して`"data": []`となる現象から、現在のFacebookアカウントが対象のFacebookページの管理者権限を持っていない、または再認証時に明示的な許可が行われていないことが原因であることを案内。
  - ユーザーがFacebookページ連携を完了し、CRMに必要な2つのキーを取得して設定完了したことを確認。
- **プログラムのバグ修正**
  - `UrlFetchApp.fetch` で `Invalid argument` エラーが発生したため、リクエストURL内の `{` や `}` をURLエンコード（`encodeURIComponent`）するように `FollowerUpdateService.gs` を修正し `clasp push` で反映。
- **本番実行エラー（アクセストークンの無効化）のトラブルシュート**
  - `updateAllFollowers` の実行にて、「Error validating access token: The session was invalidated previously using an API call. (code: 190, error_subcode: 463)」が大量発生する問題を確認。
  - パスワード変更やログアウト、時間経過、または「短期アクセストークン」のまま設定してしまったことが原因（セッション無効化）であると推測し、グラフAPIエクスプローラにて「長期アクセストークン」を生成・延長し直して、シートのB10セル（Config）に上書きする手順を案内。
