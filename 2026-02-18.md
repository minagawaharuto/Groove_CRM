# 2026-02-18 作業日報

## 実施内容

### Phase 1: フロントエンド4ファイル作成

- **index.html** — ベースHTML（ナビゲーション + #app + テンプレートインクルード）
- **style.html** — CSS変数・ツーカラムレイアウト・テーブル・トースト・タイムライン・モーダル
- **components.html** — 5画面テンプレート + 2モーダル（案件作成・候補者追加）
- **app.js.html** — hashchangeルーター、callServer APIラッパー、全画面ロジック

### Phase 2: 追加機能4つ実装

#### 1. 次アクション期限切れ一覧

- `components.html` に「🔴 期限切れ」「🟡 今日」「🟢 今週」クイックフィルタボタン追加
- `app.js.html` にトグルロジック（active/非active切替）追加
- `IndexService.gs` の `searchPeople()` に `nextActionFilter` パラメータ追加
- `_passNextActionFilter()` 関数で Date 比較フィルタリング

#### 2. validateData() 強化

- 既存実装（person_id欠落/重複 + URL重複）を確認 → 要件を満たしていた
- Admin画面テーブル表示も既存で対応済み

#### 3. CacheService 最適化

- `Main.gs` に `CACHE_KEY_SEARCH_PREFIX` 定数追加
- `clearIndexCache()` を強化：Indexチャンク（0-9）+ 検索クエリキャッシュを一括削除
- `IndexService.gs` に `_buildSearchCacheKey()` / `_cacheSearchResult()` 追加
- 検索結果をクエリ別にキャッシュし、トラッキングキー一覧で管理

#### 4. 週次バックアップ

- `WeeklyBackup.gs` 新規作成
- `createWeeklyBackup()` — `ss.copy()` で複製、ファイル名は `CRM_Backup_yyyyMMdd`
- `setupWeeklyBackupTrigger()` — 毎週月曜6時のトリガーを登録（重複防止付き）
- `removeWeeklyBackupTrigger()` — トリガー削除関数

#### btn-outline CSS追加

- `style.html` にクイックフィルタボタン用のアウトラインスタイル追加
- active時に `primary-600` 背景色に変わるトグル表現

## 変更ファイル一覧

| ファイル        | 変更内容                                                     |
| --------------- | ------------------------------------------------------------ |
| Main.gs         | `CACHE_KEY_SEARCH_PREFIX` 追加、`clearIndexCache()` 強化     |
| IndexService.gs | `searchPeople()` に nextActionFilter、クエリ別キャッシュ追加 |
| components.html | クイックフィルタボタン3つ追加                                |
| app.js.html     | state.nextActionFilter、ボタントグルロジック追加             |
| style.html      | btn-outline スタイル追加                                     |
| WeeklyBackup.gs | 新規作成（週次バックアップ）                                 |

## 次回予定

- スプレッドシートIDの設定と動作テスト
- GASエディタへのデプロイ
